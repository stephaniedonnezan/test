# Linear issue title prefix automation

This repository contains a small helper for Linear automations. When an issue
status changes to `to research`, the helper builds an `update_issue_title`
action that prefixes the issue title with `Cursor researching`.
