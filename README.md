# Linear research title prefix automation

Adds a `Cursor researching` prefix to a Linear issue title when an issue status
change event moves the issue to `To Research`.

Run tests with:

```sh
python3 -m unittest -v
```
