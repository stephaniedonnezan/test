# Linear issue title automation

Adds `Cursor researching` to a Linear issue title when a status-change
automation moves the issue to `to research`.

Run tests with:

```bash
python3 -m unittest -v
```
