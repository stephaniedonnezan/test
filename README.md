# Linear issue title prefix automation

Builds a Linear issue title update action when an issue status changes to
`to research`.

When the trigger matches, the automation prefixes the issue title with
`Cursor researching` and leaves already-prefixed titles unchanged.
