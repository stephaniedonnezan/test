# Linear title prefix automation

This repository contains a small helper for Linear issue status-change
automations. When an issue status changes to `to research`, the helper returns a
title-update action that prefixes the issue title with `Cursor researching:`.

Run the tests with:

```bash
python3 -m unittest
```
