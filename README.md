# Linear issue title automation

Builds a title update action when a Linear issue status changes to `to research`.
The generated title is prefixed with `Cursor researching` and existing prefixes
are left untouched.
