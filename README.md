# Linear issue title prefix automation

Builds an issue title update when a Linear issue status-change event moves an
issue to `to research`. Matching events receive the `Cursor researching` title
prefix once.
