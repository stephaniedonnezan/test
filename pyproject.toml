[project]
name = "linear-webhook-utils"
version = "0.1.0"
description = "Utilities for Linear webhook title transformations"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
